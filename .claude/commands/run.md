---
description: Build and launch EdgePad
allowed-tools: Bash(./build.sh run), Bash(./build.sh)
---

# Build and launch EdgePad

1. Run `./build.sh run`
2. Confirm the app launched (check Activity Monitor / `pgrep EdgePad`)
3. Remind me to grant Accessibility permission on first launch
