---
description: Build EdgePad into a .app bundle
allowed-tools: Bash(./build.sh *), Bash(./build.sh), Read
---

# Build EdgePad

Build the project using the project's build script:

1. Run `./build.sh` from the project root
2. Report any Swift compilation errors with the offending file and line
3. If it succeeds, remind me the output is at `build/EdgePad.app`

Do not edit any code — this is build-only.
